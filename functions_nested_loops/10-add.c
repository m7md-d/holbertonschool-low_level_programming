#include "main.h"

/**
 * add - Adds two integers.
 * @x: The first integer to be added.
 * @y: The second integer to be added.
 *
 * Return: The sum of the two integers (x + y).
 */
int add(int x, int y)
{
	return (x + y);
}
